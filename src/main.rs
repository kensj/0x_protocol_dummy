use ethers::{
    // abi::Abi,
    // utils::Solc,
    // types::{Address, H256},
    // contract::Contract,
    providers::{Provider, Http},
    // signers::Wallet,
};
// use std::convert::TryFrom;

fn main() {
    // connect to the network
    let client = Provider::<Http>::try_from("http://localhost:8045").unwrap();
}
